version: "3.8"

services:
  web:
    build: .
    container_name: help-gpt
    ports:
      - "8080:8080"
    environment:
      # !!! НЕ храните реальный ключ в репозитории
      OPENAI_API_KEY: "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"   # ключ OpenRouter
      MODEL: "anthropic/claude-3.5-haiku"  # или, например: anthropic/claude-3.5-sonnet
      APP_NAME: "help-gpt"
      APP_URL: "http://localhost:8080"
    restart: unless-stopped
    # УБРАЛ том, чтобы не затирать код. Если нужен — маунтите другую директорию.
    # volumes:
    #   - appdata:/data

# volumes:
#   appdata:
